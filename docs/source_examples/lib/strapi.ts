/* Storybook stub of src/lib/strapi.ts — the storybook only needs the base
 * URL that helpers/image-url.ts prefixes onto media paths. The real module
 * also constructs the StrapiClient, which has no place in a static docs
 * build. */
export const strapiUrl = 'https://strapi.azure.codecave.network'
