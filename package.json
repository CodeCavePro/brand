{
  "name": "@codecavepro/brand-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace root. Not published — the publishable package is packages/brand.",
  "license": "Unlicense",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @codecavepro/brand",
    "check": "npm run check --workspace @codecavepro/brand && npm run check:ports && node docs/tools/check-tw-bridge.mjs",
    "check:ports": "tsc --noEmit --project docs/storybook/ports/tsconfig.json",
    "check:captures": "node docs/tools/check-captures.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "dompurify": "3.4.13"
  }
}
